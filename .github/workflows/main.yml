name: GeneratePdf

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - name: Check out current repository
        uses: actions/checkout@v2
        with:
          ref: SoSe2020
      - name: Set environment variables
        run : |
          echo ::set-env name=FILELIST::$(ls *.md)
          mkdir output
      - name: Show variable
        run : |
          echo $FILELIST
      - name: Run Pandoc for a specific file
        uses: docker://pandoc/latex:2.9
        with:
          args: --standalone -f markdown --output=output/script.pdf ${{ env.FILELIST }}
      - uses: actions/upload-artifact@master
        with:
          name: output
          path: output
